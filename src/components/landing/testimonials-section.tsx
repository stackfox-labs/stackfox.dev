// Testimonials section is intentionally disabled for early stage; keeping the
// component as a no-op preserves import stability if referenced elsewhere.
export function TestimonialsSection() {
  return null
}
